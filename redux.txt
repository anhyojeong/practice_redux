// 사용자가 추가 버튼을 클릭

// handleAddMemo 함수 호출

// handleAddMemo 함수에서 addMemo 액션을 디스패치

// addMemo 함수는 리덕스 스토어에서 'ADD' 액션을 디스패치

// 'ADD' 액션은 리덕스 스토어의 리듀서 함수로 전달

// 리듀서 함수는 'ADD' 액션에 대한 처리를 수행(새 메모를 memos 배열에 추가)

// addMemo 함수에는 두 개의 객체가 인수로 전달
// id: 메모의 고유 식별자
// text: 사용자가 입력한 메모 텍스트

// 'ADD' 액션이 디스패치되어 리듀서가 호출 리듀서는 현재 액션의 유형('ADD')을 확인하고 상태를 업데이트

// 리듀서가 메모를 memos 배열에 추가하고 업데이트된 상태는 리덕스 스토어에 저장